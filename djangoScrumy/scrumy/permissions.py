from rest_framework import permissions


# class IsDeveloper(permissions. BasePermission):
# 	def has_permission( self, request,view):
# 		if self.context['request'].user.groups.filter(id = 1):
# 			return True
# 		return False

# class IsAdmin(permissions. BasePermission):
# 	def has_permission( self, request, view):
# 		if self.context['request'].user.groups.filter(id = 2):
# 			return True
# 		return False

# class IsQA(permissions. BasePermission):
# 	def has_permission( self, request, view):
# 		if self.context['request'].user.groups.filter(id = 3):
# 			return True
# 		return False

# class IsOwner(permissions. BasePermission):
# 	def has_permission( self, request, view):
# 		if self.context['request'].user.groups.filter(id = 4):
# 			return True
# 		return False
